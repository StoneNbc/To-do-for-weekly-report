# 项目开发入口

## 开始任务

- 默认只读本文件和 `documents/PROJECT_STATE.md`；上下文已有内容不重读。
- 执行 `git status --short`、`git log -3 --oneline`，核实检出并保护现有修改。
- 按索引用 `rg` 查代码、测试和文档章节，不全文预读 PRD、设计、交接或并行策略。
- 本规则替代旧全量预读和 Wave 分工要求。默认单 Agent；用户明确要求时才并行。

## 必须保留的约束

- TXT 是任务数据唯一事实来源。允许完全重复任务，禁止按正文定位或去重；未知行原样保留。
- 会话定位使用 `revision + line`，不持久化任务 ID；revision 变化返回 `FILE_CHANGED`，不猜测目标。
- 标题与连续 `  |` 详情行作为任务块编辑、删除和归档；周记与周报只投影标题，详情不进入远程发送范围。
- 未完成任务跨日保留；正常完成使用 Main 时钟，历史补录使用所选日期；周文件使用 ISO week-year。
- 归档先写周文件，成功后再写 `today.txt`；现有方案不保证严格跨文件事务或幂等。
- Renderer 不访问文件系统、不 import Main。共享类型、IPC 与 Preload 变更须同步实现及相关测试；并行时由协调者统一修改共享契约。
- 周报只写用户选择的位置，不创建内部副本、导出记录或提醒；默认不联网，远程请求须遵循现有授权与网络策略。
- 关闭悬浮窗只隐藏，托盘退出才结束程序；保留紧凑态控件占位和菜单恢复行为。
- 保留无关工作区修改；禁止全仓库机械格式化。未经用户明确要求不推送、发布或上传。
- 用户要求自己执行合并、推送和自动构建时，使用 [`documents/release-command-template.md`](documents/release-command-template.md)：只做实时预检、填入变量并给分段命令，不重复展开稳定发布机制。

## 按需导航

- 任务分类总入口：[`documents/development-index/README.md`](documents/development-index/README.md)；各模块页只保存稳定代码地图和精确文档章节。
- 详细资料均在 `documents/`；用 `rg --files documents` 按模块页给出的主题定位，不全文预读。

## 开发索引读取顺序

1. 先读本文件与 `documents/PROJECT_STATE.md`，用实时 Git 状态覆盖其中可能过期的快照。
2. 按任务打开 [`documents/development-index/README.md`](documents/development-index/README.md)，只选一个主模块；跨边界时再读其“修改联动”指向的模块页。
3. 以模块页的关键符号为起点用 `rg` 核实源码和测试；仅当需求、边界或验收不明确时，才读模块页列出的具体文档章节。
4. 索引只提供定位，不作为实现事实；若索引与源码、测试冲突，以当前检出代码和测试为准并修正索引。

## 索引增量维护

- 新增、删除、重命名入口或关键符号，改变跨层调用、测试归属、文档章节或修改联动范围时，同次变更更新对应模块索引；仅函数内部实现变化不更新。
- 出现新开发任务类别时先扩充总索引；只有形成独立调用边界后才新增模块页，避免按文件堆叠目录。
- 模块索引不记录完成度、版本、提交、临时风险和验证结果；这些覆盖写入 `PROJECT_STATE.md`。状态文件不重复代码地图，只链接相关模块索引。
- 维护后用 `rg`/路径检查确认文件、符号、测试及标题存在，并运行 `git diff --check`；纯索引改动不运行应用或业务测试。

## 验证与信息同步

- 默认按[人机协作流程](documents/协作开发流程.md)执行；本节替代旧文档中由 Agent 完成全部交付门禁的分工。
- Agent 负责实现、必要测试代码、改动审查及最小相关检查；用户负责最终 `pnpm typecheck`、`pnpm lint`、`pnpm test`、`pnpm build`、GUI 和安装验收。命令以 `package.json` 为准；用户明确委托时 Agent 可执行全量验证。
- 同一代码状态的已通过检查不重复运行；失败先定位修复，再跑受影响检查。禁止通过删测试、放宽断言或改变正确预期来消除失败。
- 交付区分“已实现／Agent 已验证／待用户验证”；给出命令、3–5 条相关人工验收项和失败反馈格式。用户反馈记录为“用户报告通过”，不得写成 Agent 实测；未验证不算验收完成。
- 小修改在当前任务直接完成；大功能设计定稿后写简短交接，建议用户在同一项目新建开发任务。不得自行新建任务或为换任务中止已授权开发；同一缺陷优先在当前任务闭环。
- 交接复用状态文件，只保留目标、范围、决定及文档章节、验证状态、下一步，不复制聊天历史。小功能不默认新增两份长文档；长期规则只在所属文档展开，其他位置引用。
- 纯文档改动只检查变更片段格式、链接与 `git diff --check`，不启动应用或重跑业务测试；避免格式化旧文档中的无关段落。
- 工具输出只保留定位片段与结果摘要；失败时再展开必要日志。搜索排除依赖、构建产物和个人数据，除非任务需要。
- 每次实质修改结束、准备切换任务或长任务的可验证里程碑，更新 `documents/PROJECT_STATE.md`：完成项、未完成项、关键决定、验证命令与结果、下一步。
- 状态采用覆盖更新，不追加对话流水账；保留更新时间和证据基线，目标不超过 80 行／4,000 字符。本文件目标不超过 80 行／5,000 字符。
- 用户可见变化同步 README／CHANGELOG；契约、架构、验收变化同步对应详细文档。只更新受影响的章节，长期决定写回所属设计文档并在状态中留引用。
- 状态文件是导航摘要；代码、测试结果与实际 Git 状态用于核实事实，PRD 表达预期。冲突须说明；旧测试数字、发布记录不能当作本次验证。
- 多个检出目录通过用户授权的 Git 提交／同步传递资料，各分支状态可能不同；同目录并行时仅协调者汇总状态。新任务应检查现场，不能假设其他对话已完成同步。
- 纯咨询且未改变项目决定或实现时，不为记录聊天而改状态。中断恢复时先检查 diff 和状态，不能假设上一任务已写完交接。
