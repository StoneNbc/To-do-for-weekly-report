# 变更日志

本项目按语义化版本记录面向用户的重要变化。

## 未发布

## 0.1.6 - 2026-09-15

### 新增

- 按项目管理待办：创建、筛选、分组、颜色、排序、归档／恢复及空项目删除。
- TXT 直接保存项目名；旧文件首次写入项目时备份升级，当前和历史任务、完成／重新打开及日归档保持项目归属。
- 项目统一改名支持影响预览、精确字段替换、备份和启动恢复；遇外部修改冲突保留原文并阻止继续写入。
- 周记和周报支持项目筛选、日期／项目分组及 `{{project_records}}` 模板变量。

### 调整

- 接入、断开或调整显示器时保留有效的贴边隐藏状态；所在显示器被移除后迁到主屏同侧，边缘成为屏幕接缝时安全恢复可见。
- 按用户选定的方案 2 实现 macOS 风格：浅色分层项目卡片、名称旁数量、浅蓝圆形“＋”、蓝色主按钮和自适应底部输入区；创建／管理窗口同步样式。视觉验收待用户完成。
- 新配置默认背景改为珍珠灰，并增加对应预设；保留已有配置中的背景色，默认值与测试 mock 同步。
- 新建项目使用独立窗口，重复打开复用实例；创建成功后选中新项目并聚焦待办输入框。
- 主页移除“整理”入口，保留单任务所属项目编辑。
- 全部项目视图保留零待办的进行中项目，项目行新增“＋”快捷添加按钮，已完成区不显示空项目组。
- 当前未完成待办作为下周计划候选改为显式选择、默认关闭，只纳入所选项目。
- 远程生成先离线展示发送素材，素材或设置变化拒绝过期预览；授权绑定服务 origin 与隐私声明版本。不发送项目目录和任务详情。

### 文档

- 同步项目管理功能设计、开发文档、模块索引及使用说明。

## 0.1.4 - 2026-09-04

### 新增

- 设置中新增默认关闭的“贴边自动隐藏”开关，支持便利贴停靠屏幕左侧、右侧或顶部。
- 隐藏后保留 2 DIP 边缘提示条，鼠标靠近即可恢复完整窗口。
- 设置中新增“隐藏提示条颜色”，支持预设色和系统自定义取色器。
- 设置中新增“在全屏应用中显示”开关；保持置顶开启时，便利贴可跨 macOS Space 显示在常规全屏应用上方。

### 修复

- 鼠标停留在可拖拽标题栏时，便利贴不会被误判为鼠标离开而再次隐藏。
- 已展开的便利贴开始手动拖动时立即解除旧停靠状态，拖离边缘后不会再次缩回。
- macOS 切换桌面或全屏 Space 后，左右隐藏态不再被系统恢复成大块白色空窗。
- macOS 使用原生悬浮面板并保持隐藏提示条在全屏应用上方可访问。
- 自动保存外观设置时不再短暂灰化控件或插入占位内容，避免设置页闪烁和布局跳动。

### 验证

- 39 个测试文件、199 项测试通过。
- TypeScript、ESLint 和 Renderer/Main/Preload 生产构建通过。
- macOS 实机验证提示条、标题栏悬停和展开后拖离边缘行为通过；2 DIP 与自定义颜色待本轮手工验收。

## 0.1.3 - 2026-08-30

### 新增

- 待办支持多行详情编辑，详情默认收起并可按任务独立展开。
- 详情随今日完成、跨日归档、历史日期完成和重新打开完整保留。
- 整个便利贴支持紧凑收起，只保留标题栏、日期导航、菜单和恢复按钮。
- 收起后移动窗口，再展开会在新位置恢复收起前的完整尺寸。

### 改进

- 同一时间只允许编辑一条任务，标题与详情提供明确的保存和取消操作。
- 上下展开箭头统一为同一个 SVG 旋转，尺寸、线宽和位置保持一致。
- 收起和展开沿用相同标题栏布局，避免日期、菜单和按钮发生位置跳动。
- 紧凑状态保留三个点菜单；点击时自动恢复窗口并打开菜单。

### 数据与隐私

- Today 和 Week TXT 使用 `  | 内容` 保存详情，旧文件无需迁移。
- 详情不是子任务，不参与完成状态、数量统计和排序。
- 周记、模板周报和远程 LLM 输入继续只使用任务标题，不包含详情。

### 验证

- 37 个测试文件、179 项测试通过。
- TypeScript、ESLint 和 Renderer/Main/Preload 生产构建通过。

## 0.1.2

- 支持历史日期查看全局待办，并在指定历史日期完成或重新打开任务。
- 接入正式应用图标、Dock 图标、窗口图标和高分辨率托盘图标。

## 0.1.1

- GitHub Release 自动构建并上传 macOS 和 Windows 安装包与 SHA-256 校验文件。

## 0.1.0

- 首个可分发版本，包含今日待办、自动归档、历史周记和 TXT 周报导出。
