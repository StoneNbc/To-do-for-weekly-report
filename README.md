# 悬浮便利贴 & 一键周报

完全本地运行的 Electron 桌面便利贴应用。当前处于 Wave 0 工程初始化阶段。

## 开发命令

```bash
pnpm install
pnpm dev
pnpm typecheck
pnpm lint
pnpm test
pnpm build
```

## 数据位置

- 开发环境：项目根目录下的 `data/`
- 生产环境：Electron `userData/data/`

项目使用纯本地 Git，不配置远程仓库，不上传代码。
